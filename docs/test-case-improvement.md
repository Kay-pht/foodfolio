| 優先度      | 種別                       | 不足テスト                             | 必要なケース                                                                                                                                         | 理由                                                                                                                                 |
| -------- | ------------------------ | --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| **P0-1** | Integration              | **Workerの同時実行・lease・処理権**         | 同一pendingを2 Workerが同時claim→1つだけAI開始 / 有効lease中は別Workerが処理しない / stale leaseのみreclaim / reclaim後の旧Workerは結果を書けない / 旧Workerはpending・failedにも戻せない  | **最重要。現在のmain実装は `attempt > 1` ならprocessingをそのままreclaimでき、run所有権もありません。**  オープンPR #3でも、まさにこの6ケースをIntegration Test対象として明記しています。     |
| **P0-2** | Integration              | **認証・他ユーザーアクセス防止**                | Authorizationなし / 空Bearer / token検証失敗→401、User AがUser BのRecipeをGET/PATCH/DELETE→404、他ユーザーRecipeへのtag操作拒否                                       | 現状はforeign tag attachのみテストされています。Recipeそのものの所有権境界は未検証です。APIは全Recipe操作でuserIdを使う設計なので、セキュリティ境界として必須です。                              |
| **P0-3** | Integration              | **解析状態による編集制御**                   | `pending`→PATCH 409 / `processing`→409 / `completed`→成功 / `failed`→成功                                                                          | 「解析中は編集不可、完了・失敗後は編集可」はMVP仕様そのものです。現在のIntegration testはcompleted成功しかありません。                                                          |
| **P0-4** | Integration              | **レシピ保存と解析開始失敗の分離**               | Cloud Tasks enqueue失敗→Recipe自体は残る / HTTP 201 / status=failed / failure titleになる                                                                | 「AI解析失敗でも保存済みRecipeを失わない」というMVPの重要設計です。実装コードには処理がありますがテストされていません。                                                                  |
| **P0-5** | Integration              | **アカウント削除の完全cascade + 再試行**       | Recipe・Ingredient・Step・Tag・RecipeTag・DeviceToken・Settingを作成→DELETE `/v1/me`→全削除 / Firebase削除が1回目失敗→2回目retry成功 / retry時にDB Userを再生成しない          | 現在のテストはUser行が消えることと2回DELETEできることまで。**子データ全部の消滅とFirebase失敗→retry** が不足しています。                                                        |
| **P0-6** | Integration              | **差分同期の境界条件**                     | 同一`updatedAt`の複数Recipeをidで取りこぼさない / sync処理中にhigh-waterより後で更新されたRecipeは次回syncに出る / malformed cursor→400                                         | 同期はBackend DB→SwiftDataの整合性の根幹です。現在は「1回sync→次回空」までで、cursor境界の取りこぼしを検証していません。APIは `(updatedAt,id)` + high-water方式なので、ここを直接テストすべきです。 |
| **P1-1** | iOS Integration / UI E2E | **オフライン利用**                       | ネットワーク切断状態でも保存済み一覧・詳細・検索・genre/tag絞り込みが可能 / Add・Edit・Tag変更・Delete失敗時にローカルデータを勝手に変更しない                                                          | オフライン閲覧は明示されたMVP要件です。現在のUIテスト用HTTP stubは常にAPI応答するため、offline状態を検証していません。                                                            |
| **P1-2** | iOS Integration          | **ローカル検索条件の分解テスト**                | title一致 / ingredient一致 / 複数語AND / genreのみ / tagのみ / query+genre+tag / 不一致 / 空白query                                                            | 現状は `"鶏肉 玉ねぎ" + genre + tag` の複合正常系が中心です。検索ロジック自体は複数条件をANDしているため、条件ごとに1回ずつ境界を確認した方がよいです。                                           |
| **P1-3** | UI E2E                   | **人数・分量表示の仕様境界**                  | servingsなし→UIなし / `1〜2人分`等で数値基準なし→rawだけ表示・±なし / 1人でminus disabled / 20人でplus disabled / 詳細を開き直すと基準人数に戻る / 数値量だけ変化し「少々」は不変                      | 現在は2→3人とfraction表示まで。仕様上重要な「変更不可条件」と1/20の境界が未テストです。実装にも1〜20の境界があります。                                                               |
| **P1-4** | UI E2E                   | **analysisStatus別の画面状態**          | pending/processing→編集ボタンなし / failed→失敗表示・Recipe残存・編集可能 / completed→通常表示                                                                        | Backend側の状態テストとは別に、ユーザーに見える挙動を1本のparameterized相当のUIテストで確認すべきです。                                                                    |
| **P1-5** | Integration              | **入力validation境界**                | title=空/200文字/201文字、genre=有効/null/不正、ingredients=配列以外/ingredient name空、tag=1/30/31文字                                                           | 正常・異常・境界という要求を満たす上で必要です。現在のコードには200文字、Tagには30文字という明確な境界がありますが、境界値テストがありません。                                                        |
| **P1-6** | Integration              | **DeviceToken / notification異常系** | token登録→再登録idempotent / 別userで同一token登録→所有者移行 / 他userからDELETEしても消えない / FCM invalid token返却→DB token削除 / 通知send失敗でもRecipe completed/failed状態を維持 | Pushは外部依存なので、正常系よりもDBとの整合性をstubで保証するのがMVP向けです。invalid token削除処理と通知エラー握りつぶし処理が現状未検証です。                                              |
| **P1-7** | iOS Integration          | **ローカルデータ・画像cleanupの結合**          | Recipe delete→SwiftData + Application Support画像削除 / hard reconciliation→消えたRecipe画像も削除 / logout/account delete→Recipe・Tag・画像・cursor・検索履歴削除     | `RecipeImageStore`単体のstore/removeはありますが、「Recipe削除と一緒に消える」ことまでは検証されていません。Repositoryにはその責務があります。                                     |
| **P2-1** | Integration              | **Recipe一覧pagination**            | limit=1 / default30 / 50 / 51指定→50 / cursor後に次ページ / createdAt同値時のid ordering                                                                   | APIに明確な1〜50境界があるため最低限1本のtable-driven testが妥当。ただしMVPの主要事故リスクは上より低いです。                                                               |
| **P2-2** | Unit                     | **APIError mapping拡充**            | 422 validation / 409 analysis-in-progress / 500 / decode不能response / transport offline                                                         | 現在はduplicateと401中心です。表示メッセージの退行防止として有用ですが、Backend Integrationより優先度は下です。                                                            |
| **P2-3** | Unit                     | **SearchHistoryStore個別削除**        | 複数件から1件だけ削除 / 存在しない値を削除しても他を維持 / 空文字add無視                                                                                                      | 実装は単純ですが、PR #15で個別削除UIが追加されているためUnitを1本足す価値があります。PR #15側にはUI E2Eは追加済みです。                                                           |
