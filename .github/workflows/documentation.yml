name: Documentation (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Documentation checks are integrated into Quality."
