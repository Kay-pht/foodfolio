name: TikTok Video Download PoC PR

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This PoC moved to local execution: npm run poc:tiktok-video"
